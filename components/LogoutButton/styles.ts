import React from 'react';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  logoutButton: {
    position: 'absolute',
    alignSelf: 'flex-end',
    right: 20,
    top: 60,
    zIndex: 1000,
  },
});
